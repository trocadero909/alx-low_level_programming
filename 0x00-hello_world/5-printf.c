#include <stdio.h>
/**
 * main - A program that print a line the printf function
 * RETURN: 0 (success)
 */
int main(void)
{
:wqprintf("with proper grammar, but the outcome is a piece of art,\n") 
return (0);
}
