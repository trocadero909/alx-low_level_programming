#inlude "main.h"

/**
 * print_alphabet_10 - function that will print  the alphabet 10 times 
 *
 * */
 void print_alphabet_x10(void);
{
        int line. ch;
        for (line = 0; line <= 9; line++)
	{
		for (ch = 'a'; ch <= 'Z'; ch++)
                _putchar[ch]);
        _putchar('\n');
       }}

